import { PrefixfornamePipe } from './prefixforname.pipe';

describe('PrefixfornamePipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixfornamePipe();
    expect(pipe).toBeTruthy();
  });
});
